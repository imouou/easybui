loader.define(function(requires, exports, module, global) {

    var pageview = {};

    // 模块初始化定义
    pageview.init = async function() {

    }

    // 初始化
    pageview.init();

    // 输出模块
    return pageview;
})